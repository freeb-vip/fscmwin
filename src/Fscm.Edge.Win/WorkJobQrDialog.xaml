<Window x:Class="Fscm.Edge.Win.WorkJobQrDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="作业二维码" Width="740" Height="610" MinWidth="680" MinHeight="580"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="#F5F7FA" ShowInTaskbar="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="18" />
      <RowDefinition Height="*" />
      <RowDefinition Height="18" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding OperationDisplay}" TextWrapping="Wrap" />
      <TextBlock Margin="0,5,0,0" Foreground="#667085" Text="扫描对应二维码开始或结束当前扫描器的作业" />
    </StackPanel>

    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="18" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border Padding="18" Background="White" BorderBrush="#D0D5DD" BorderThickness="1" CornerRadius="6">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock HorizontalAlignment="Center" FontSize="17" FontWeight="SemiBold" Text="开始作业" />
          <Border Grid.Row="2" Padding="8" HorizontalAlignment="Center" Background="White">
            <Image Width="240" Height="240" Stretch="Uniform" SnapsToDevicePixels="True"
                   RenderOptions.BitmapScalingMode="NearestNeighbor" Source="{Binding StartQrImage}" />
          </Border>
          <TextBox Grid.Row="4" Padding="4" HorizontalContentAlignment="Center" Background="Transparent" BorderThickness="0"
                   Foreground="#667085" IsReadOnly="True" Text="{Binding StartPayload}" />
        </Grid>
      </Border>

      <Border Grid.Column="2" Padding="18" Background="White" BorderBrush="#D0D5DD" BorderThickness="1" CornerRadius="6">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock HorizontalAlignment="Center" FontSize="17" FontWeight="SemiBold" Text="结束作业" />
          <Border Grid.Row="2" Padding="8" HorizontalAlignment="Center" Background="White">
            <Image Width="240" Height="240" Stretch="Uniform" SnapsToDevicePixels="True"
                   RenderOptions.BitmapScalingMode="NearestNeighbor" Source="{Binding EndQrImage}" />
          </Border>
          <TextBox Grid.Row="4" Padding="4" HorizontalContentAlignment="Center" Background="Transparent" BorderThickness="0"
                   Foreground="#667085" IsReadOnly="True" Text="{Binding EndPayload}" />
        </Grid>
      </Border>
    </Grid>

    <Button Grid.Row="4" Width="88" Padding="14,7" HorizontalAlignment="Right" IsDefault="True" Content="关闭" Click="OnCloseClick" />
  </Grid>
</Window>